{
  "build": {
    "docker": {
      "dockerfile": "Dockerfile"
    }
  },
  "deploy": {
    "install": [
      {
        "script": "docker-compose up -d postgres redis"
      },
      {
        "script": "npx prisma migrate deploy"
      }
    ]
  }
}
